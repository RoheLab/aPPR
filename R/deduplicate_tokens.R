# deduplicate_retweet_tokens <- function() {}
#
# get_tokens()
#
#
# class(rtweet:::load_tokens(rtweet:::twitter_pat()))
#
# fs::dir_ls("~/Documents") #/Documents/*.rds")
#
#
# token_paths <- list.files(
#   "~/../Documents",
#   pattern = ".rtweet_token*",
#   all.files = TRUE,
#   full.names = TRUE
# )
#
# tokens <- purrr::map(token_paths, readr::read_rds)
#
#
# length(tokens)
# length(unique(tokens))
#
# tokens[[1]]$credentials$oauth_token
#
